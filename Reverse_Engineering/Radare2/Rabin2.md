# Rabin2
* * *

```
rabin2 -e binaryName # shows entrypoints

rabin2 -i binaryName # shows imports

rabin2 -zz binaryName # shows strigns

rabin2 -g binaryName # shows all info

rabin2 -s bianryName # shows sections
```
